# ============================================
# 283バドミントン 出欠管理アプリ 環境変数
# ============================================

# ローカル開発用 (SQLite)
# DATABASE_URL="file:./dev.db"

# Azure SQL Database用
# DATABASE_URL="sqlserver://SERVER_NAME.database.windows.net:1433;database=DB_NAME;user=USERNAME;password=PASSWORD;encrypt=true;trustServerCertificate=false"

# NextAuth設定
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# LINE Login (LINE Developers Console から取得)
LINE_CHANNEL_ID="your-line-channel-id"
LINE_CHANNEL_SECRET="your-line-channel-secret"

# ============================================
# Azure App Service 環境変数設定
# ============================================
# Azure Portal > App Service > 構成 > アプリケーション設定 に以下を設定:
#
# DATABASE_URL         : Azure SQL Database接続文字列
# NEXTAUTH_URL         : https://your-app-name.azurewebsites.net
# NEXTAUTH_SECRET      : 安全なランダム文字列 (openssl rand -base64 32)
# LINE_CHANNEL_ID      : LINE Developers Console から取得
# LINE_CHANNEL_SECRET  : LINE Developers Console から取得
#
# ============================================
# LINE Developers Console 設定
# ============================================
# コールバックURL（ローカル）: http://localhost:3000/api/auth/callback/line
# コールバックURL（DEV）    : https://your-app-name.azurewebsites.net/api/auth/callback/line
